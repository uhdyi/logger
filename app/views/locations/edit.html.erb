<h1>Editing Location</h1>

<% form_for(@location) do |f| %>
  <%= f.error_messages %>
  
  <!--
  <p>
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </p>
  -->
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<!-- <%= link_to 'Show', @location %> | -->
<%= link_to 'Back', locations_path %>
